export interface IAbout {
    map(arg0: (item: any) => any): import('react').ReactNode;
    id: 15;
    name: 'Our Team & Guide';
    content: string | null;
    assetList: {
        id: number;
        assetURL: string;
        tag: string;
        name: string;
    };
}
